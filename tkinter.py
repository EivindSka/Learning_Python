from tkinter import *

# widget = GUI elemtes: buttons, textbox, label, image
# windows= serves as a container to hold or contain these widgets

window = Tk()  # instanciate a instance of  a windows
window.geometry('420x420')
window.titlefunction('Window Windows')

window.mainloop()
